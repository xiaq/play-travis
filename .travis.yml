language: go
os:
  - linux
  - osx
sudo: false
go:
  - "1.11"
  - "1.10"
env: CGO_ENABLED=0
jobs:
  include:
    - stage: coverage
      script: echo coverage
      if: type = push
      os: linux
    - stage: coverage
      script: echo coverage
      if: type = push
      os: osx
    - stage: deploy
      script: echo deploying for branch = $TRAVIS_BRANCH
      if: type = push
script: echo test
